# 终端布局位置优先：主控与交叉审查

## 主控初审（2026-09-26）

被审提交：4e9d1fad6ce1b75450068b1af58864ef8d4b4d11；开发 worktree ../saddle-worktrees/pane-placement，agent saddle/dev-pane-placement-1。主控只审本轮位置优先改动，New 和 Agents 样式旧任务不重审。

结论：暂不通过。代码范围和鼠标主流程、会话移动、取消不改布局的现有断言核对通过；标准套件有一项失败，须先定位修复，尚未进入独立交叉审查、合并或发布。

### 必须改 1：取消回归在标准套件中失败

- `cargo test --all-targets` 本轮共 133 passed、1 failed、2 ignored；失败为 `placement_cancel_and_escape_never_attach_and_new_cancel_keeps_the_draft`，`tests/workflow.rs:1210` 等待 `Open agent in a new tab` 超时。失败画面停在普通 Agents/Viewer，无候选弹框；事件记录只有 ls/status，无 attach/start/stop。
- 完整主控日志 `/tmp/saddle-placement-main-tests.log`；随即仅复跑此用例，通过（1 passed），日志 `/tmp/saddle-placement-cancel.log`。因此存在未定位的时序/同步问题，不能将单次复跑通过作为修复完成。
- 交原开发者定位产品还是测试同步问题，做最小修复。若是辅助函数或屏幕等待竞态，只改必要同步；若是实际输入被吞，修对应产品问题。保留所有原草稿保留、取消、零 attach/start/stop 断言，不加固定 sleep、重试、扩大超时或删除断言来掩盖。
- 增量预算：只检查失败用例及受修复影响的回归；受影响 workflow 测试目标一次，必要时一个直接复现探针由开发者决定。若修改 Rust 产品代码则运行 Clippy；diff 检查。无需重复整套 all-targets、录屏或大矩阵。主控复核只看修复增量及受影响检查。

### 取舍逐项裁决

1. 入口迁到右侧、删除旧 Show/o：接受，符合批准流程。
2. 窄窗优先 Split/Close pane，Close tab 保留标签条 ×；小框贴发起窗格、列表按名字排序且滚动：接受，局部展示取舍，不扩大范围。
3. 分屏排除发起窗格所持有的显示/等待/替换目标，tab 允许移动当前项：接受；保持普通列表点击跳转与布局选择移动的区别。
4. 移动整个 Pane，保留稳定 ID/Viewer/会话/请求修订号，来源分屏合并、空 tab 移除：接受该实现方向；交叉审查继续核对异步边界和无重复接入。
5. 弹框模态阻止用户关闭发起窗格，既有异步路径不删窗格，因此无额外失效框架：接受当前调用链解释；交叉审查核对前提。
6. 开发待裁决 1——状态失败或外部已接入后保留预留空位：本轮接受沿用既有失败处理。批准范围明确保证“取消”无空布局，未要求失败自动回滚；不为此追加需求或阻挡合并。失败自动收回若用户后续要求另做。
7. 开发待裁决 2——保留 `Tab N` 与 `‹+ Tab›`：接受。线框用于说明入口位置，用户批准流程未要求重做 tab 命名；加号入口已有明确文字。
8. 本轮开发自行补充小屏渲染检查已完成，不再扩大覆盖或重复。新增局部 placement 模块及必要原函数拆分接受，未扩展通用框架。

Clippy `--all-targets -- -D warnings` 与 `git diff --check main...HEAD` 通过。原 T2 已放行，不操作队列、不执行 go/next；待办 T14 按钮样式不在本轮实施。

## 第一轮返工通知

只处理上述必须改 1；完成后在开发分支的 docs/任务/终端布局位置优先.md 追加根因、修复增量、验证结果并提交。不得修改本主仓库审查文件或合并推送。回复 SHA 与是否有待裁决事项，最后一行 DONE。

## 第一轮返工主控增量复核（2026-09-26）

复核范围仅 4e9d1fa..48a5a488ff474995d143abe4411215195994e25d：2 个文件，产品代码零变化，tests/workflow.rs 加一处同步及注释，开发任务追加根因记录。

- 必须改 1：通过，关闭。原 `see` 可立即匹配屏幕缓存中的旧状态行；新增等待草稿消失，建立 Esc 已处理的可观察条件，再保留原状态断言。核对测试读屏按输出块更新，修复与所述旧屏幕竞态一致；未删断言、未增 sleep/retry/超时。
- 主控受影响 `cargo test --test workflow` 一次通过：35 passed、0 failed、2 ignored；日志 /tmp/saddle-placement-recheck-workflow.log。增量 diff 检查通过。产品未改，不重复之前通过的 Clippy 或整套 all-targets。初审其他目标全部通过，本轮受影响目标已补齐。
- 接受局部测试同步修复。开发关于“真实终端不会同次读取”的表述不作为通过依据；本次判据是等待真实界面状态变化、保留功能断言和受影响检查通过。
- 主控审查现通过，进入独立交叉审查；此前 8 项实现取舍裁决保持，不重开已放行的 T2/New/Agents 样式或待办 T14。

## 独立交叉审查任务

2026-09-26，saddle/main 交给 saddle/dev-pane-review（Codex，重：gpt-6-astra / xhigh）。本任务是只读审查，不再委派。

### 先读与位置

- AGENTS.md；本文件的主控初审、逐项裁决和增量复核。
- 自己 worktree 内 docs/任务/终端布局位置优先.md、DESIGN 第 29 节，必要时参考第 27 节生命周期规则。
- worktree：/Users/firegnu/Developer/personal_projs/saddle-worktrees/review-pane-placement，detached 固定在 48a5a488ff474995d143abe4411215195994e25d。开始先核对 HEAD。
- 代码范围：在自己的 worktree 看 `git diff 039b8d8..HEAD`；重点 src/placement.rs、src/terminals.rs、src/app.rs、src/ui.rs 和相关测试。不得改代码、测试、设计或任务文件，不提交、不切分支、不合并、不推送。
- 唯一可写文件：主仓库 /Users/firegnu/Developer/personal_projs/saddle/docs/任务/终端布局主控与交叉审查.md，只追加「独立交叉审查意见」。不要在 detached worktree 写同名文件。

### 重点

1. 右侧 Split/+ Tab 的正常鼠标路径及模态输入：先选位置再选 agent，取消不改布局；候选列表点击、滚动、异步刷新时是否选中正确目标，是否透传键鼠。
2. Move here 真正移动现有会话，不重连、不重复 attach、不 stop；保留屏幕内容、稳定身份，来源整理和活动窗格有效。
3. 正在接入、待定替换或 start 结果等异步状态与移动/关闭的归属，确保不把输入或结果交给错误 agent/窗格。
4. take/insert/claim 重用对普通查看、关闭回收的影响，以及“弹框期间发起窗格仍存在”的前提；原必须改 1 的同步修复是否保留有意义断言。
5. 对照批准流程和任务边界审范围，逐项评价本文件 8 项取舍；失败预留空位和 Tab N/+ Tab 已由主控裁决接受，不把未批准的额外 UX 功能当必须改。

### 验证预算和约束

主控标准检查及受影响复核已有记录，不重复整套。可运行直接相关的 terminals 检查和上述 3 条新 workflow 主路径，以及有明确怀疑时的小范围合成探针（临时副本或 /tmp，不改被审 worktree）。不要录屏、大矩阵、泛化扫描、无关缺陷注入。Cargo 一律共享 `CARGO_TARGET_DIR=$HOME/Developer/personal_projs/saddle-worktrees/.target`。
不操作真实 agent、队列、用户配置、release，不读 corral/drover 内部文件或改其仓库；只用公开接口/假 CLI/临时合成数据。不运行 go/next，不按项目名批量杀进程。

### 输出

追加「独立交叉审查意见」，先写可以合并/改完再合并；每项标必须改/建议改/可以不改，给代码位置、触发、后果及最小改法，并对 8 项取舍表态。必须改仅限批准流程正常使用不达标或真实生命周期/异步/输入路由缺陷，罕见组合与范围外边角记建议，不扩需求。证据明确区分源码推断与实际探针。
回复只写结论、各级条数，命令均前台完成，最后一行 DONE。


## 独立交叉审查意见（2026-09-26）

结论：**改完再合并**。必须改 1 条，建议改 0 条，可以不改 9 条（主控 8 项取舍及原取消回归修复）。

审查者 saddle/dev-pane-review；自己的 detached worktree HEAD 已核对为 `48a5a488ff474995d143abe4411215195994e25d`，审查范围 `039b8d8..HEAD`。以下代码位置均指这个固定版本。没有修改被审代码、测试、设计或任务文件，没有提交、切分支、合并或推送；只追加本审查段落。

### 必须改 1：候选列表刷新会把已按下的点击提交给另一个 agent

- **位置**：`src/placement.rs:176–182` 给所有候选行生成同样的 `KeyCode::Null` 命中信息，动作只携带 `Pick(index)`；`src/app.rs:703–713` 松开时按当前位置寻找动作，`src/app.rs:487–489` 再从最新候选列表取 `nth(index)`。既有 `src/buttons.rs:70–82` 只比较 `(Focus, Hit)`，其中没有 agent 名字，所以候选换人而矩形不变时仍视为同一按钮。
- **正常触发**：打开 `+ Tab` 候选框，在 `p/b` 上按下鼠标；尚未松开时，后台公开 ls 刷新得到新 agent `p/aa`，它按名字插入 `p/b` 前面；原处松开。无需关闭窗格、异常数据或故障注入，只需正常候选增减发生在一次鼠标手势期间。
- **实际探针**：在 `/tmp/saddle-placement-review-1nk8_mdl` 使用当前 worktree 的源码与假 CLI 副本，新增 `review_candidate_refresh_does_not_retarget_a_pressed_row`。探针等待 `p/b` 按下后的前景色真实改变，才往临时 `agents.json` 加入 `p/aa`；再等待弹框实际显示 `p/aa`，确认它占据原坐标，然后松开。结果不是测试等待超时：程序打开了 `p/aa`，事件记录出现 `attach p/aa`、`input p/aa 5a`，没有 `attach p/b`，最后明确失败于“mouse-up must not open a different name”。完整证据在该目录 `refresh-probe.log`，探针位于副本 `tests/workflow.rs:1795` 起。产品源码和其余测试与被审 worktree 逐文件相同，只有副本 workflow 末尾追加探针。
- **后果**：用户点击的目标被后台刷新替换，随后输入进入另一个 agent；若替代候选已经打开，同一路径会错误移动它的会话。这属于批准鼠标流程的目标身份/输入归属缺陷，须阻挡合并。后者“错误移动”由相同 `pick → place` 分支推断，本次实际探针验证的是错误接入和字符输入。
- **最小改法**：让一次候选点击绑定按下时的 agent 名字，松开时核对同一身份；候选被移走、删除或该坐标换人时取消这次手势，不能提交另一名字。确认动作应使用已绑定且仍合法的名字，避免再用最新列表的 `nth(index)` 解释旧点击。只需在 placement 的点击状态/命中路径做局部修复，不要求新通用框架。补上“按下 → 实际列表刷新 → 松开”的定向回归，保留现有取消、移动和零重复 attach/stop 断言；不靠 sleep、重试或增加超时掩盖。

### 可以不改 1–8：主控取舍逐项复核

| 编号 | 取舍与判断 | 代码、触发及结果；最小处理 |
|---|---|---|
| 1 | **可以不改**：入口迁到右侧、删除 Show/o，接受。 | `src/app.rs:538–562`、`src/app.rs:893` 起、`src/ui.rs:365` 起。Split/+ Tab 先建立 placement，只有 `pick` 才调用布局放置；左侧普通查看仍走 `activate_existing`。三条 workflow 实测覆盖右侧鼠标主路径；无需恢复旧入口。 |
| 2 | **可以不改**：窄窗按钮优先级、小框位置、名称排序及滚动方式，接受。 | `src/terminals.rs:567–605`、`src/placement.rs:55`、`:96–115`、`:150–158`、`src/app.rs:516–530`、`:732–748`。候选列表稳定时，滚轮与键盘更新选择索引，绘制和行命中使用同一段可见索引；Close tab 的标签 × 仍在。布局取舍无需改；刷新中的点击身份单独按必须改 1 修复。滚动映射为源码核对，本轮没有另跑长列表矩阵。 |
| 3 | **可以不改**：分屏排除发起窗格所持名字、tab 允许移动当前项，接受。 | `src/placement.rs:46–52`、`src/terminals.rs:225–234`。`find` 同时覆盖 requested、Viewer target 和 showing；正在显示/等待/替换的自身名字不会作为自身分屏候选。tab 保留当前项以执行移动。第一、第二条 workflow 实测自身排除和 Move here 标识；无需调整规则。 |
| 4 | **可以不改**：移动整个 Pane、来源整理和稳定异步归属，接受。 | `src/terminals.rs:244–318`、`:347–387`，`src/app.rs:307–370`，`src/viewer.rs:81–130`。移动已有会话只 take/insert，不经过 close；同名待接入请求随 Pane 移走，票据继续按 ID/revision 查找。选择当前显示项认领该窗格时会按既有规则取消待定替换/start，旧票据失效；关闭移动后的窗格则移除该 ID，Viewer 进入回收集合。后台 start 的已关闭/已替换目标不会 stop 新 agent。实测会话内容与输入、单次 attach、无移动期 detach，以及 status 等待中移动后的结果归属通过；start/PTY 完成及关闭分支为源码核对，无需重构。 |
| 5 | **可以不改**：弹框期间发起窗格不会被删除，接受。 | `src/ui.rs:143–158` 替换底层命中区；`src/app.rs:610–612`、`:658–665`、`:732–749` 拦截键盘、粘贴及其他鼠标；`:565–566` 是用户关闭入口。异步 `tick`、`disappeared`、Viewer 退出只更新/释放会话，不删除 Pane；New 表单与 placement 也不能经正常输入同时可见。故 anchor 存在的前提成立，无需新增失效框架。底层光标亦被 `src/ui.rs:79` 禁用。此项是调用链核对；现有 workflow 另验证原生控件没有鼠标透传。 |
| 6 | **可以不改**：失败或外部已接入时保留预留空位，接受既有裁决。 | `src/app.rs:307–327`、`src/terminals.rs:351–361`。选定后 status 失败/attached elsewhere 会清除请求而保留目标 Pane；取消则在 `place` 前结束，尚未预留布局。取消无空布局实测通过；失败自动回收不在本轮要求内，不作为缺陷或追加验收。 |
| 7 | **可以不改**：保留 Tab N 与 ‹+ Tab›，接受。 | `src/terminals.rs:481–505`。移动/删空来源 tab 后标签按当前顺序显示，内部身份仍使用稳定 ID；文字没有阻碍批准流程，三条 workflow 的 tab 路径通过，无需改名。 |
| 8 | **可以不改**：局部 placement 模块与必要辅助函数拆分、小屏检查及范围，接受。 | `src/placement.rs`、`src/terminals.rs:236–387`；本轮 `src/launch.rs` 只删除旧 Show 绘制并调整对应样式测试，未重做 New。`claim` 保留普通查看的认领语义，`take` 供移动和关闭共用，关闭仍回收自有 attach，最后一个窗格关闭会补空 tab。运行既有 terminals 目标时其小屏检查一并通过，没有另加小屏矩阵；无需扩大到 T2、Agents 样式、T14、Queue 或布局框架。 |

### 可以不改 9：原取消回归同步修复保留有效断言

`tests/workflow.rs:1204–1237`：先确认重开的草稿出现，再在 Esc 后等待草稿消失，随后仍检查原状态行；取消后还等待候选框消失并断言没有 Tab 2，最终仍断言零 attach/start/stop。`4e9d1fa..48a5a48` 的该测试增量仅新增同步及注释，没有删断言、sleep、retry 或放大超时。由 `Harness::pump` 按输出块更新屏幕的方式看，这个条件能排除旧状态行直接满足等待的问题。接受主控已通过的受影响 workflow 复核；本轮未重跑该整目标，也不以“真实终端不会同次读取”作为证明。无需继续返工此项。

### 本轮实际验证与边界

所有命令均前台等待结束；所有 Cargo 命令均设置 `CARGO_TARGET_DIR=$HOME/Developer/personal_projs/saddle-worktrees/.target`。

- 自己 worktree：`cargo test --test terminals`，**6 passed**。
- 自己 worktree：`cargo test --test workflow -- split_and_new_tab_choose_the_place_before_the_agent_and_cancel_leaves_no_layout choosing_an_open_agent_moves_its_session_without_attaching_again moving_an_attaching_agent_keeps_its_request_with_the_moved_pane --nocapture`，**3 passed、34 filtered out**。实际合成终端输出可见两步菜单、Move here、取消后布局不增加、移动后的历史输出及等待 status 的结果归属。
- 临时副本：`cargo test --test workflow review_candidate_refresh_does_not_retarget_a_pressed_row -- --exact --nocapture`，**0 passed、1 failed、37 filtered out**，退出码 101；失败点和真实错误输入事件见必须改 1，属于有效缺陷证据。
- `git diff --check 039b8d8..HEAD` 通过；最终 HEAD 仍为指定提交，`git status --porcelain` 无输出、`git symbolic-ref -q HEAD` 为 detached。未重复 all-targets 或 Clippy；未启动/接入/停止真实 agent，未操作真实队列、用户配置、release 或 corral/drover 内部文件。

收尾核对：主仓库 `git status --short` 仅本审查文件有改动，被审 worktree 仍干净；追加前原文 8092 字节逐字节保留，审查文件 diff 检查通过。临时探针函数的精确入口为 `/tmp/saddle-placement-review-1nk8_mdl/tests/workflow.rs:1798`。

## 交叉初审主控裁决与第二轮开发返工（2026-09-26）

结论：接受交叉审查「改完再合并」。下述「交叉必须改 1」与已关闭的主控初审取消同步问题区分编号；本次只返工候选点击身份，不重开原同步问题。

- 交叉必须改 1：成立，必须修。主控读了探针函数与 refresh-probe.log，确认探针先等按下反馈，再等真实候选刷新占据同一坐标；日志实际出现 attach p/aa 与 input p/aa 5a，错误目标得到输入。源码确实只携带索引且 Hit 身份相同，松开后再用最新 candidates.nth(index)，因此不是虚构竞态或单纯测试超时。无需重复探针来证明同一事实。
- 可以不改 1：接受，入口和批准主路径一致。
- 可以不改 2：接受布局与滚动取舍；刷新中的点击身份归入唯一必须改项。
- 可以不改 3：接受自身排除及 tab 移动规则。
- 可以不改 4：接受 Pane 整体移动、来源整理和稳定请求身份；本次缺陷不否定已验证的会话移动实现。
- 可以不改 5：接受 modal 输入和 anchor 存在前提的调用链核对。
- 可以不改 6：接受既有失败预留空位，不追加失败自动回滚。
- 可以不改 7：接受 Tab N/+ Tab 文案，不改命名。
- 可以不改 8：接受局部模块拆分和当前范围，不扩到 T14/Queue/New/Agents。
- 可以不改 9：接受原取消同步修复，继续保持关闭。
- 建议改：无。交叉 terminals 6 项、三条新 workflow 通过记录接受，不重跑已完成验证。

### 交开发者的增量修复范围和预算

仍由 saddle/dev-pane-placement-1 在原 pane-placement 分支/worktree 修复交叉必须改 1。只对 placement 鼠标候选提交建立按下目标身份，松开必须核对同一名字且仍合法；原坐标换人、删除或移动导致身份不一致时取消该次手势，不能接入/移动另一个 agent。最终提交应使用绑定名字，不用最新索引重新解释旧点击。采用最小局部修改，不要求通用按钮框架重构。

保留现有行为断言，补定向“按下 → 列表实际刷新 → 原处松开”回归；正确取消后弹框可以保留，测试等待应按正确行为同步，不能机械保留缺陷探针里“必然关闭弹框并 READY”的步骤。可只读参考 /tmp/saddle-placement-review-1nk8_mdl/tests/workflow.rs:1798 和 refresh-probe.log。用真实可观察条件建立 RED/GREEN，不加 sleep/retry/超时掩盖。

只跑新回归和受改动影响的候选/取消/移动工作流及直接相关检查，加 Clippy 与 diff 检查；不重复整套 all-targets，不跑大矩阵。修改公共命中路径时，验证其实际受影响入口即可，不扩张修复范围。测试用假 CLI 和临时数据，不操作真实 agent/队列/配置/release。

完成后在开发分支任务末尾追加第二轮返工记录并提交，回复 SHA 与待裁决项，最后一行 DONE。主控复核只看 48a5a48 之后增量；通过后必须先将 detached review-pane-placement checkout 到新 SHA，再通知同一 saddle/dev-pane-review-1 只复核交叉必须改 1 及修复引入的问题。这是交叉审查后的第一次返工/复核，尚未发生交叉复核轮次。

## 第二轮开发返工主控增量复核（2026-09-26）

范围仅 48a5a48..73b3d88e1be4b63f7924add002fa80e5454495a2。结论：交叉必须改 1 的修复主控通过，交原审查者增量复核后才关闭阻挡。

- `Placement.pressed` 保存按下时 agent 名字；松开时取出并与当前位置的合法候选名字核对，最终用绑定的名字调用 place。按下时刚绘制的候选和事件处理间无异步状态更新，松开时与最新候选核对可阻止刷新换人；公共 Pointer/Hit 不变。Enter 在同一事件里绑定当前高亮名字再走同路径。原有功能断言保留，初始化新增字段属必要接线。
- 新定向回归确实等待按下配色、候选换位及松开配色；验证换人后弹框保留、未创建目标布局，并验证刷新但原名字仍在原行时正常打开，最终无错误目标 attach/input，正确目标仅一次 attach、无 stop。接受此局部修复和对应设计补充，不扩通用框架。
- 主控检查：lib 6 passed、terminals 6 passed；workflow 35 passed、1 failed、2 ignored，其中新身份回归和原位置/取消/移动/等待接入等受影响检查全部通过。Clippy `--all-targets -- -D warnings` 及增量 diff 检查通过。日志 /tmp/saddle-placement-second-recheck.log。
- 范围外观察：上述唯一失败为未改动的 `native_mouse_buttons_cover_forms_and_stop_confirmation`，在 tests/workflow.rs:475 等待 Ctrl-] 回 Agents，画面仍为 Queue operation completed；它没有打开 placement，新增鼠标分支仅在 placement 存在时执行，queue/buttons/input 文件及该用例原代码本轮均未改动。随后仅复跑此用例 1 passed（/tmp/saddle-placement-unrelated-check.log）。不将混合目标称为全绿，也不把单次复跑当根因修复；目前按与本次增量无关的偶发检查问题记录为建议另查，不重开 Queue/Stop。请交叉复核静态确认本增量关联性即可；若发现直接关联的新缺陷再列必须改。
- 审查 worktree 已实际 checkout --detach 到 73b3d88，HEAD 完整 SHA 已核对、工作区干净；开发者保留原 worktree 等待。本轮不重复 all-targets 或旧任务审查。

## 第一轮交叉增量复核通知

仍由 saddle/dev-pane-review-1（Codex gpt-6-astra / xhigh）只审 48a5a48..73b3d88，自己 detached review-pane-placement HEAD 必须为 73b3d88e1be4b63f7924add002fa80e5454495a2。只复核交叉必须改 1 是否修复和修复是否引入问题；原取消同步与原 9 项接受结论不重审。唯一可写仍是主仓库本文件，只追加「第一轮交叉增量复核意见」，不改代码或提交。

验证预算：新点击身份定向回归及直接相关候选/取消/移动检查，必要时一个明确怀疑的小探针；不重跑整套、无矩阵、不操作真实环境。主控新记录的范围外用例偶发失败按上文只核对关联性，不扩大为 Queue 专项。输出逐项结论及条数，先写可以合并/改完再合并，区分必须改与建议改；命令前台完成，最后一行 DONE。
