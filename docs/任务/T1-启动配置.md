# T1：增加配置文件，启动时从配置文件读取配置

2026-09-25，saddle/main 交给 saddle/dev-t1-config（Codex，常规：gpt-6-astra / high）。
路由：常规 / 交叉审查不要 / 影响面：改行为（路由：档位拿不准、交叉审查不要、影响面拿不准；按现有配置扩展的行为改动确定）。项目要求主控审查。
你是被委派的 agent：照本文件做，不要再开别的 agent。

## 先读

- AGENTS.md。
- docs/DESIGN.md 第 8 节配置，以及第 17、18、21 节当前颜色约定。
- src/config.rs、src/main.rs、src/theme.rs，以及 src/ui.rs、src/queue.rs、src/buttons.rs 中的配色使用。
- README.md 和 README.zh-CN.md 的配置部分。

## 在哪里干活

- worktree：/Users/firegnu/Developer/personal_projs/saddle-worktrees/t1-config，分支 t1-config，已从 main 建好。
- 范围：本任务的配置加载、主题配色及其直接调用点、相关测试/示例、默认配置示例文件、配置文档和本任务完成记录。不改 corral 或 drover。
- 所有 cargo 命令加 CARGO_TARGET_DIR=$HOME/Developer/personal_projs/saddle-worktrees/.target。

## 要做的

用户原话：

> 看一下有哪些配置可以提取到配置文件中，配置文件的位置按照通用的配置文件位置，配置文件不需要非常复杂，我理解主要就是颜色配置等。不要太复杂，但是又不能明显可以配置的却没有配置，然后程序改为启动时从配置文件加载配置。配置文件中的配置可以按照现在的程序中的数据进行默认配置

现状与范围：

- 现有 TOML 已支持 corral、left_width、left_split、refresh_ms、queue.drover、queue.cwd，启动时读取 ~/.config/saddle/config.toml，支持 --config。沿用这些配置和已有语义，不另造一套。
- 默认位置支持有效的绝对路径 XDG_CONFIG_HOME 下的 saddle/config.toml，未设置或不适用时沿用 ~/.config/saddle/config.toml；--config 保持优先。没有文件和省略的配置项继续采用当前默认值。
- 当前明显缺少的是硬编码颜色：界面基础色、边框/焦点/文字层次、选中背景、连接/未读/操作反馈、Agents 和 Queue 状态强调色、四种 agent 类型色。提取为简单的颜色配置；默认呈现与当前一致，支持终端默认色、ANSI 色名和 #RRGGBB 即可。不引入多个主题、热加载或复杂继承。
- 检查上述相关显示路径，确保配置实际用于启动后的界面。Viewer 中 agent 输出自己的终端颜色不受主题覆盖。字体字号属于外部终端，不作为 saddle 配置；超时、轮询内部步长等实现细节不都暴露为选项。
- 提供可直接复制到标准位置的完整默认 config.toml 示例，简短注释说明用途、颜色写法和可省略项；更新中英文配置说明与 --help 的配置路径说明。沿用当前解析错误报告方式。
- 先更新 docs/DESIGN.md 的配置约定再实现，提交说明这是用户 T1 要求的颜色配置扩展。完成记录简要列出提取的配置，以及明显候选但未提取的理由。

## 怎么算做完

> 增加配置文件，启动时从配置文件读取配置
>
> 看一下有哪些配置可以提取到配置文件中，配置文件的位置按照通用的配置文件位置，配置文件不需要非常复杂，我理解主要就是颜色配置等。不要太复杂，但是又不能明显可以配置的却没有配置，然后程序改为启动时从配置文件加载配置。配置文件中的配置可以按照现在的程序中的数据进行默认配置

验证预算：先用针对配置到界面生效的自动检查取得真实 RED，再最小实现到 GREEN；项目标准 cargo test --all-targets 和 cargo clippy --all-targets -- -D warnings 各跑一次，另做 git diff --check。遵守 AGENTS.md 的轻量 TDD。测试使用临时配置、假 CLI 和合成数据；无需真实 agent、录屏或缺陷注入。预算不够时在回复说明。

## 不要做

- 不改 Queue 翻页和隐藏 Reply 等无关遗留行为，不顺手调整默认视觉效果。
- 不修改用户真实配置、真实队列、服务或用户 agent，不读取 corral/drover 内部文件。默认配置示例放在仓库，主控收尾时处理用户配置文件。
- 不按项目名或路径批量杀进程；只停自己记录 PID 的进程。
- 遇到必须改变现有交互或新增复杂配置体系才能推进的问题，停下来报告。
- 不合并 main，不推送，不更新 HANDOFF.md。只在 t1-config 分支提交。

## 做完

在本文件末尾追加「## 完成记录」（在你的分支里提交）：做了什么、验证了什么、拿主意的地方、没做的事，各几句话。回复里只写这几样，加上有没有要主控决定的事。命令都在前台跑完，全部做完后，回复最后一行写 DONE。

## 完成记录

- 做了什么：先更新 DESIGN 第 8 节及第 17、18 节的配置化说明，再实现启动配置扩展。保留现有 corral、布局、刷新和 Queue 配置语义，新增绝对路径 XDG_CONFIG_HOME 支持及原 HOME 路径回退，--config 优先。增加平铺 `[colors]` 的 27 项颜色：基础背景/弹层、文字层次/边框/焦点、选中背景、连接/未读/危险操作、Agents 与 Queue 共用状态/操作反馈、四种 agent 类型，以及输入目标文字和保留的回复格式。主题显式传给所有相关渲染与按钮路径，Viewer 输出颜色独立。补齐根目录 config.toml 完整默认示例、中英文 README 和 --help。
- 验证了什么：启动到界面的 PTY 检查先真实 RED（`unknown field colors`），实现后 GREEN，观察 Agents、Queue、按钮、弹层的自定义 RGB，并验证 Viewer 的默认色及 agent 输出的 ANSI/RGB 原色。XDG 路径检查先 RED（错误读取 HOME 配置），实现后覆盖绝对路径、未设置、空值、相对路径和 --config 优先级。按钮配色相等时的悬停检查先 RED（Red 而非配置的 bright），改为按按钮危险标记判断后 GREEN。解析检查覆盖 16 种 ANSI 名、默认色、混合大小写 RGB、非法颜色/未知字段、缺省项/缺失文件和错误路径；完整示例默认值与程序默认值相同，27 个颜色字段齐全。
- 标准验证：所有 cargo 命令均使用 `CARGO_TARGET_DIR=$HOME/Developer/personal_projs/saddle-worktrees/.target`，在前台完成。针对性 `cargo test --test buttons --test layout_config --test ui` 通过。`cargo test --all-targets` 跑一次：64 passed、2 ignored（既有真实 drover CLI 测试，本任务不启用）；`cargo clippy --all-targets -- -D warnings` 跑一次通过；`git diff --check` 通过。PTY 测试子进程清空继承的 NO_COLOR，以实际检查颜色输出；未改变产品对该环境变量的既有行为。
- 拿主意的地方：采用单个可省略的颜色表，不引入全局可变主题。Queue 沿用与 Agents 共用状态强调色；按钮背景继承所在面板/弹层，悬停和按下保留背景。按钮危险性改为显式标记，避免用户把边框等配置成危险色时误判。无需主控另作产品决定。
- 没做的事：字体字号归外部终端；Viewer 的 agent 终端调色板归输出本身；超时、内部事件轮询步长等是实现细节，未提取。未增加多主题、继承或热加载，未修改 Queue 翻页或隐藏 Reply 行为；未改用户配置、真实队列、服务、用户 agent 或 corral/drover 仓库。未分派，未合并/推送 main，未更新 HANDOFF.md。默认配置安装留给主控收尾。

## 主控审查

- 结论：可以合并。核对 6b0d5bb 全部改动，配置路径、缺省值、27 项颜色及实际渲染接入符合 T1；未扩展到 tab/split 或 Tasks 分组配色等后续任务。
- 主控复跑标准检查：cargo test --all-targets 为 64 passed、2 ignored；cargo clippy --all-targets -- -D warnings 通过，git diff --check 通过。配置默认值与完整示例一致，启动到界面检查覆盖自定义色生效和 Viewer 原色保留。
- 同意单个可省略颜色表、Queue 与 Agents 共用状态色、显式传入主题、按钮危险标记独立于配色、按钮继承背景的取舍。字体和内部超时不纳入配置，保持简单。
- 无阻挡合并的问题；已有 --help 的 r reply 和 Queue PgUp/PgDn 遗留仍留待独立任务。本轮不操作用户 agent 或重启正在运行的界面。
