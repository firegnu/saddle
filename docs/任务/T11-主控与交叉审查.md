# T11：主控与交叉审查

2026-09-26，saddle/main 委派 saddle/dev-t11-review（Codex，重：gpt-6-astra / xhigh）只读交叉审查。
本任务按开发任务路由要求交叉审查；你是被委派的审查者，不再开 agent。

## 背景与主控初审

- 开发者 saddle/dev-t11-git-1 在 t11-agent-git 提交 66e7ca0，实现每个 agent 所在 worktree 的分支、提交差值、未提交 diff 和未跟踪文件数。用户批准方案见 docs/任务/T11-AgentsGit摘要.md。
- 主控已看实现与完成记录；标准测试 89 passed、2 ignored，cargo clippy --all-targets -- -D warnings 与 git diff --check 通过。
- 暂不能合并：主控用临时仓库独立复现 `.git/info/attributes` 配置的 clean filter 会被当前采集命令执行。临时仓库有已提交 a.txt，设置 filter.probe.clean 为 touch 标记后 cat 的脚本，info/attributes 写 `*.txt filter=probe`，修改 a.txt 后运行当前 `--no-optional-locks -c core.fsmonitor=false --attr-source=<空树> diff-index --numstat -z --no-ext-diff --no-textconv HEAD --`，结果标记文件存在。违反任务「避免运行仓库配置的外部辅助程序」及只读边界，不能仅记为文档限制后通过。
- 开发者另外报告：旧版 Git 忽略 GIT_NO_LAZY_FETCH 时，部分克隆仍可能补取对象；任务已批准只读本地、不联网，应核实支持版本与拒绝路径，不能把运行时 fetch 当成允许的取舍。
- 主控初步接受：数字归属 worktree、同根去重、二进制独立计数、无 cwd 不显示、未知字段用 —、固定 5 秒和 PATH git。Git 最低版本与禁用 attributes 对统计语义的影响待核实后定，不要求猜测或掩盖不支持的情况。

## 先读

- AGENTS.md；docs/任务/T11-AgentsGit摘要.md；docs/DESIGN.md 第 25 节；本文件。
- src/git.rs、src/command.rs、src/agents.rs、src/app.rs、src/ui.rs；相关 tests/git.rs、tests/agents.rs、tests/ui.rs。

## 审查位置与边界

- 你自己的 detached worktree：/Users/firegnu/Developer/personal_projs/saddle-worktrees/review-t11-agent-git，固定提交 66e7ca0。
- 代码范围：`git diff 02d8896..66e7ca0`。只读，不改代码、不提交、不切分支。
- 唯一可写文件：本文件的主仓库绝对路径 `/Users/firegnu/Developer/personal_projs/saddle/docs/任务/T11-主控与交叉审查.md`，在末尾追加「## 交叉审查意见」。本文件不在你的 worktree 中。
- 所有 cargo 命令加 CARGO_TARGET_DIR=$HOME/Developer/personal_projs/saddle-worktrees/.target。
- 可运行直接相关的 git/agents/ui 测试及临时合成仓库/假 git 的定向复现；主控已跑全套，不再重跑无关套件。不录屏、不做覆盖矩阵或缺陷注入。
- 不运行真实 saddle 采集用户项目；不改用户配置/队列/登记、真实仓库、任何 agent；不读 corral/drover 内部文件。不为验证联网；如需测试网络边界，用本地可观察替身，不访问真实远端。不按名字/路径批量杀进程。

## 审查重点

1. 只读/外部程序/网络边界：主控确认的 info/attributes 问题；全局 attributes、filters、Git 配置或继承环境是否还会造成真实副作用。Git 版本兼容失败是否如实表示，不虚构全版本保证。
2. 后台并发与退出：取消、超时、目录变更、积压结果、慢仓库、进程生命周期是否会阻塞界面/退出或污染结果；区分正常使用缺陷与罕见组合的建议。
3. 归属：同 worktree 根/子目录去重，不同 worktree 隔离；cwd 变化、agent 消失重开及晚到结果的处理是否符合已批准语义。
4. 数字准确性：提交基准、HEAD 工作区净差异、未跟踪、二进制、无 HEAD/detached/失败，及禁用 attributes 对结果含义的代价；不要把未知当 0。
5. 界面和范围：每条摘要紧跟目录、宽窄换行与颜色、已有信息及交互保留；测试是否真正覆盖主路径，是否越界或引入不必要架构。

## 输出

先写一句「可以合并 / 改完再合并」。意见分必须改、建议改、可以不改；每条给位置、触发条件、影响、证据与修法方向。必须改只用于任务未达到或真实核心/安全缺陷；清单外罕见边角记建议改。逐条评价开发者取舍，避免以扩大功能作为通过条件。
回复只写结论和各级条数，全部命令在前台结束，最后一行 DONE。


## 交叉审查意见

改完再合并。

2026-09-26，saddle/dev-t11-review：审查固定提交 `66e7ca0b9aee75491919c90028772222ed12684d`，范围 `02d8896..66e7ca0`。共 **必须改 4 项、建议改 3 项、可以不改 5 项**。源码行号指这个提交；完成记录中声明的限制不自动成为用户批准的例外。

### 必须改

1. **[P1] 阻止全部 attributes 来源的外部 filter，不能只屏蔽工作区 `.gitattributes`。**

   - 位置：`src/git.rs:140-152,96-105`；`tests/git.rs:158-185`；`docs/DESIGN.md:274`。
   - 触发与影响：已跟踪且修改过的 `a.txt`，配置 `filter.probe.clean=<脚本>`，在 `.git/info/attributes` 写 `*.txt filter=probe`，或者用 `core.attributesFile` 指向同样的规则。后台摘要会执行配置脚本，脚本可写文件或联网，违反只读及不运行外部辅助程序的要求。`--no-ext-diff --no-textconv` 不禁止 clean/process filter，空树 attr-source 也未覆盖全部来源。
   - 证据：用本提交未修改的 Git/command 源码编译临时 harness，调用真实 `collect`；info 和 core.attributesFile 两个夹具都生成了脚本标记，摘要仍返回 `+1 -0`。独立确认主控复现，并补出 global attributes 路径。现有测试只在工作区 `.gitattributes` 写规则，绿色不能证明其名称中的 “never run repository helpers”。
   - 修法方向：在读取文件内容前，使所有配置来源中的 clean/process filter 无法执行；不能保证安全则返回不可用。继续禁止外部 diff、textconv、fsmonitor，不修改真实仓库配置或 attributes；补这两个来源的回归检查。开发者把 info/global filter 留作文档限制的取舍不接受。

2. **[P1] Git 支持范围必须覆盖禁止 lazy fetch 的能力，并在查询前拒绝不支持的 Git。**

   - 位置：`src/git.rs:15-16,149-152,186-206`；两份 README 第 40 行；`docs/DESIGN.md:274`。
   - 触发与影响：当前文档声称支持的旧 Git 忽略 `GIT_NO_LAZY_FETCH` 时，部分克隆缺对象就可能启动 promisor fetch，联网并写对象库。代码无能力/版本门禁，环境变量被忽略也不会报错；“旧 Git 会忽略”不能作为“不自动 fetch”的已批准例外。
   - 版本证据及边界：本机实际可执行版本为 Homebrew Git 2.55.0、Apple Git 2.54.0，未实跑旧版二进制。安装包附带的官方 `RelNotes/2.45.0.adoc:120-122` 记录 `--no-lazy-fetch` 的引入；`git.adoc:183-188` 明确它等价于 `GIT_NO_LAZY_FETCH=1`。因此不能仅凭 2.41 支持 attr-source 就声称兼具禁止补取能力。
   - 定向证据：临时本地部分克隆确认缺一个 blob，远端改为只写标记后失败的本地 `git-remote-probe`。2.55 下真实 collect 返回 `changes: None`，没有调用替身；同一夹具在允许 lazy fetch 的直接 Git 命令中确实调用替身并返回 128，证明观测路径有效，未访问真实远端。这不构成旧版安全的证据。另外，用假 Git 对 attr-source 返回 129、其余调用交给真 Git，得到的是 `Some(Summary { head: Unknown, ... })`，UI 会显示整行未知值，亦不符合 README 的 `git unavailable`，因为 locate 尚未检查该选项。
   - 修法方向：优先在第一条定位命令及后续命令都传能被旧版明确拒绝的 `--no-lazy-fetch`，以 Git 2.45 引入的能力为门槛；不支持则在对象统计前返回不可用，不降级成允许 fetch。若用其他机制，需给出同等拒绝证据。同步最低版本和失败显示说明，补缺对象时本地远端替身不被调用的回归检查。固定 PATH git 可以保留。

3. **[P2] 隔离会改变仓库目标的继承环境。**

   - 位置：`src/command.rs:36-46`；`src/git.rs:149-152,190-206`。
   - 触发与影响：启动环境带 `GIT_DIR`/`GIT_WORK_TREE` 时，current directory 不会覆盖这些变量；不同 agent 的 cwd 可能解析到同一外部指定 worktree并被去重，分支和数字全部归错目录。`GIT_INDEX_FILE` 等同样能改变数据来源，违反本次功能的核心归属要求。
   - 证据：独立临时仓库 A/B 分支为 `branch-a`/`branch-b`，B 另有一个未跟踪文件；只让 harness 继承指向 A 的 `GIT_DIR`、`GIT_WORK_TREE`，真实 `collect([A,B])` 就给两项都返回 `branch-a`、`?0`。另单独继承指向临时文件的 `GIT_TRACE`，一次采集实际写入 540 字节，表明 no-optional-locks 也不隔离环境副作用。
   - 修法方向：在 Git 专用路径清理或明确覆盖重定向仓库、工作区、索引、对象来源的环境变量，并约束会写文件的 trace 环境；保留必要普通环境。不要为此改变 corral/drover 的运行语义。补两个合成仓库在受污染环境下仍各读各自 cwd 的检查。

4. **[P2] 禁用全部 attributes 后，不能继续把原始字节差异称为未提交净增删。**

   - 位置：`src/git.rs:96-126,201-206`；`docs/DESIGN.md:269,274`；两份 README 的 Git 摘要说明。
   - 触发与影响：普通 `.gitattributes` 使用 `*.txt text eol=crlf`，HEAD 为 LF、工作区为 CRLF。提交后内容完全不变，仅 mtime 改变，也会被报出增删行。这改变了用户批准的“相对 HEAD 的未提交差异”，不只是 binary 识别方式的取舍。
   - 证据：合成仓库 a.txt 两行 CRLF，提交后只用 utime 改时间戳；真实 collect 返回 `added: 2, deleted: 2, binary: 0`，同仓库 `git --no-optional-locks diff --numstat HEAD --` 输出为空。另一个 `*.asset binary` 夹具中，正常 Git 返回 `- - a.asset`，采集却为 `+1 -1`、`binary: 0`，确认属性语义确实被抹掉。
   - 修法方向：结合第 1 项，尽量保留 Git 内建 text/eol/encoding 转换，只阻止外部辅助程序；依赖外部 filter 才能准确比较时宁可显示未知。若改为原始字节口径，需主控另行确认语义并明确标识，不能沿用“未提交净差异、提交后归零”的承诺。补“已提交 CRLF 内容不变但 stat 变化”的回归检查。按内容自动判 binary 可以作为明确说明的限制，但不能掩盖这里已复现的假增删。

### 建议改

1. **慢目录不宜阻挡全部已完成摘要和新目录查询。** 位置：`src/git.rs:157-183,225-243,254-258`。整轮 collect 完才发 Batch，watch 变化只排队，不取消旧目录。假 Git 让旧目录的 diff-index、ls-files 各超时 5 秒，200 毫秒后 watch 只保留健康目录；切换后首批结果约 10,490 毫秒才收到，只有健康目录的新轮次约 10,551 毫秒收到。实际周期是整轮耗时再加 5 秒。建议及时发布完成的 worktree，名单变化后尽早跳过无人使用的旧目录；单线程可保留，不要求线程池。源码显示该等待不在 corral/UI 主循环，同夹具 drop 约 10 毫秒，故列建议，不追加正常 UI 阻塞的结论。

2. **退出验证需区别直接子进程与它的后代。** 位置：`src/command.rs:40-80`、`src/git.rs:157-179`、`tests/git.rs:218-227`。现有 `exec sleep 30` 只证明能取消直接子进程；runner 只调用 child.kill，读取管道的线程未 join，等输出期间也不看 cancel。若 PATH git 包装器留下持有管道的后代，可能等待输出 deadline且不回收后代；collect 取消后仍可能启动余下命令。证据是这些源码路径，这是复用 runner 的既有限制，本次没有把它实证为普通 Git 的退出失败。建议先在 spawn 前、每个 cwd 前检查取消；若要承诺支持派生后代的包装器，再补记录 PID、能自行清理的定向检查，必要时只在 Git 调用内管理进程组。filter 边界修好后，不必为罕见包装器组合扩大本次架构。

3. **明确纯重命名的增删口径。** 位置：`src/git.rs:98-105`。临时仓库把两行文件原样 git mv 并暂存，即使 `diff.renames=true`，当前 diff-index numstat 仍是旧路径 `0/2`、新路径 `2/0`，采集显示 `+2 -2`；内容没变也产生整文件统计。建议明确保留按路径增删还是启用 rename 检测，以免与常见 git diff 口径混淆。批准方案没明确 rename 启发式，不列必须改。若启用，须同时处理 numstat -z 的旧/新双路径记录，不能把每个 NUL 片段都当统计记录。

### 可以不改

1. **按 worktree 归属和去重的基本方案成立。** `src/git.rs:157-175` 以 show-toplevel 为共享键、保留原 cwd 返回；`src/agents.rs:64-75` 仅接纳当前使用的 cwd，UI 按当前 cwd 查表。排除必须改第 3 项的环境污染后，测试证明根/子目录共享查询、同仓库两个 worktree 分开。同名重开但仍在同目录时，共享目录快照符合批准语义，不必改成 agent 实例或任务独有；换到不同 cwd 后，旧目录晚到结果会被丢弃。

2. **提交基准和未知字段选择可以保留。** `src/git.rs:66-95,127-130`：开发分支只用本地 refs/heads/main，main 用配置 upstream，无基准不猜；未跟踪文件独立计数。无 HEAD、detached、无 main、无 upstream 的测试通过。`C—`、`+— -—`、`?—`、`HEAD detached`、`git …` 合理；detached 仍可显示能确定的 diff。整条不可用与部分字段未知按必须改第 2 项对齐文档，不必全部改为 0。

3. **Git 行的位置、样式和已有交互可以保留。** `src/ui.rs:617-674,714-787` 在目录之后插入摘要，折行保留颜色，增绿删红复用主题，无 cwd 不加 Git 行。UI 测试覆盖共享数字、位置、折行、颜色、已有条目信息/effort/选择样式并通过。binary 独立计数可保留，判定语义按必须改第 4 项处理，不必增加 diff 详情页。

4. **固定 PATH git、固定间隔和公开 cwd 边界可以保留。** `src/app.rs:183,259-272` 符合范围，不需新增配置，不追踪 agent 后续 cd，不读 corral/drover 内部文件。5 秒可作为正常轮询间隔，慢查询的实际时延按建议第 1 项准确说明。

5. **独立后台 Poller、Panel 表和有限验证范围可以保留。** `src/app.rs:255-275` 把 Git 接收与 corral 更新分开，run_with_env 包装保留原 command 接口；不需增加服务层、执行追踪或无界并发。直接子进程取消、目录切换及基本归属测试有效；不要求真实用户项目、agent 操作、录屏、覆盖矩阵或缺陷注入。测试缺口应针对以上已确认问题补齐，不扩大功能。

### 本次验证与边界

- 前台执行 `CARGO_TARGET_DIR=$HOME/Developer/personal_projs/saddle-worktrees/.target cargo test --test git --test agents --test ui`：Git 4、Agents 3、UI 20，共 27 passed。未重跑主控已完成的全套测试和 clippy。
- `git diff --check 02d8896..66e7ca0` 通过。定向验证只用仓库外临时文件、合成仓库、假 Git或只写标记的本地 remote helper；harness 直接编译本提交未修改的 Git/command 源码，未启动真实 saddle。
- 未访问真实远端，未操作任何 agent，未修改真实仓库配置、队列或登记。未改代码/测试、未提交、未切分支；只在本主仓库审查文件追加本节。审查结束时清理临时夹具。

## 主控裁决与第一轮返工

2026-09-26，saddle/main：认可「改完再合并」。交叉审查的必须改 1–4 均有明确违反已批准语义的路径；第 1 项也与主控独立复现一致。交回原开发者 saddle/dev-t11-git-1，在 t11-agent-git 修复后由同一审查者增量复核；目前不合并、不推送开发改动、不关闭任一 agent 或删除 worktree。

### 返工范围（仅必须改 1–4）

1. 修复所有 attributes 来源可能执行外部 clean/process 等辅助程序的问题。不能把 info/global attributes 写成例外后通过；继续满足只读、不联网、不写用户索引/配置。
2. 在定位和对象查询前建立可拒绝的禁止 lazy fetch 能力边界。接受使用明确的 --no-lazy-fetch 及相应最低 Git 版本（审查给出的 2.45 能力起点），不支持即显示不可用，不静默降级；文档与实际失败路径一致。
3. 在 Git 专用调用中隔离影响仓库/工作区/索引/对象归属和 trace 写入的环境，不能改变 corral/drover 既有运行环境。保留两个不同 cwd 对应各自仓库的语义。
4. 保留 Git 内建 text/eol 等规范化后的未提交净差异，修复已提交 CRLF 文件仅 stat 改变却报 +2 -2 的问题。与第 1 项一起处理；依赖不允许执行的外部 filter 才能准确计算时如实未知，不改成原始字节口径，不要求用户放宽已批准定义。

### 其他取舍

- 建议改 1（慢目录推迟整批结果）：本轮不作为返工条件，保留单线程；文档准确说明查询耗时会延长实际刷新周期。
- 建议改 2（包装器后代及取消）：本轮不要求新增进程组或包装器支持；如果随本次代码改动加入 spawn/cwd 前取消检查，保持局部且说明，不扩大到 runner 重构。
- 建议改 3（纯重命名）：批准方案没定 rename 启发式，本轮保留按路径增删并在说明中明确，不增加 rename 解析。
- 接受「可以不改」1–5：worktree 归属、提交基准、未知字段、二进制单列、样式、固定 PATH/间隔、独立后台结构均可保留。前提是必须改的环境、能力与属性语义得到修复。

### 返工交付与验证

- 先读本文件的完整交叉审查意见，再改原开发 worktree；不要合并 main 或修改本主仓库审查文件。只在开发分支提交，完成记录追加到原任务文件并在回复逐条对应 1–4。
- 对已确认缺陷补针对性 RED→GREEN，使用临时合成仓库/假 Git/本地只写标记的远端替身，禁止真实网络或用户仓库试验。保留已有有效断言，不把失败条件改为允许副作用。
- 本轮只跑改动相关的 Git/Agents/UI 检查及 cargo clippy --all-targets -- -D warnings、git diff --check；仅有新失败或变动确需额外回归时扩展。不重复无关全套，不做矩阵、录屏或缺陷注入。
- 若遇到现有公开 Git 能力无法满足已批准语义的具体缺口，报告事实与最小可行范围，不擅自放宽限制或增加大型架构。
- 回复做了什么、验证结果、取舍与未完成事项，全部命令在前台结束，最后一行 DONE。

## 第一轮返工主控增量复核及复审请求

- 2026-09-26，主控核对 `66e7ca0..bb38568` 全部返工 diff，必须改 1–4 的修复方向与裁决一致，增量复核通过，最终合并仍等待交叉复核。
- 主控运行 cargo test --test git --test git_env --test agents --test ui --test corral --test drover：40 passed；cargo clippy --all-targets -- -D warnings、git diff --check 66e7ca0..HEAD 通过。没有重跑无关全套。
- 接受在 Git 专用路径移除 GIT_*、所有调用传 --no-lazy-fetch、枚举 filter 后只用命令行覆盖禁用外部驱动、保留内建 attributes 的方案；需要被阻止的 filter 才能比较时整个增删行未知，不按原始字节伪造结果。无关文件可正常计数。racy stat 需要重新转换内容时同样允许未知，driver 名含 = 无法可靠覆盖时未知。上述限制已说明，不改变已批准显示含义。
- detached 审查 worktree 已由主控更新到 `bb38568bda4dd736347914aadd802181833cf145`，状态干净。此前固定 66e7ca0 的说明只对应第一轮；本次运行测试与读源码必须基于 bb38568。
- 交给原审查者 saddle/dev-t11-review-1：只复核上一轮必须改 1–4 及返工引入的问题，审查 diff `66e7ca0..bb38568`。其余建议按主控既有裁决，不重新扩大范围。可运行直接相关的 Git/git_env 检查和必要的临时合成复现，不重跑主控刚完成的其他套件。
- 只在本主仓库文件末尾追加「## 第一轮返工交叉复核」，逐项说明 1–4 是否解决，给出可以合并/改完再合并；新发现且与返工无关的边角只记建议。不改代码、不提交、不切分支，其他只读边界不变。回复结论与条数，命令在前台跑完，最后一行 DONE。


## 第一轮返工交叉复核

改完再合并。

2026-09-26，saddle/dev-t11-review-1：只复核 `66e7ca0..bb38568`，运行时 HEAD 为 `bb38568bda4dd736347914aadd802181833cf145`。原必须改 1–4 中，**3 项已解决（2、3、4），1 项未完全解决（1）；本轮仍有必须改 1 项，新增建议 0 项**。不重新打开主控已裁决的慢目录、包装器后代、纯重命名等建议。

### 必须改 1：顶层来源已修复，但子模块路径仍会执行 filter

**[P1] 本轮返工重新启用 attributes 后，diff-index 间接进入子模块执行了未被禁用的 clean filter。**

- 位置：`src/git.rs:55-84` 只枚举当前顶层仓库的 filter；`src/git.rs:123-137` 执行 diff-index 时未限制其检查子模块工作区。`tests/git.rs:166-228` 覆盖了同一仓库的工作区/info/global 来源及 process driver，未覆盖这个间接调用。
- 触发：普通父仓库包含一个已初始化的本地子模块 `child`。父仓库没有 filter 配置；child 的 `.gitattributes` 为 `*.txt filter=subprobe`，仅在 child 的仓库配置中设 `filter.subprobe.clean=<写标记后 cat 的脚本>`。把 child 中已提交文件从 `one\n` 改成同长度 `two\n`，只对父仓库调用 collect。
- 当前结果：使用 bb38568 原样源码编译的临时 harness，真实 collect 父仓库后标记文件存在，脚本实际执行了 3 次；摘要却正常返回 `changes: Some(Changes { added: 0, deleted: 0, binary: 0 })`，并未拒绝此路径。顶层 `config --get-regexp ^filter\.` 返回 1、输出为空，因此没有子模块 driver 的覆盖参数。
- 调用链证据：单独运行实现中的同一条 diff-index 命令，在临时夹具中开启受控 Git trace，记录到 `cd child; ... git status --porcelain=2 -uno`，随后执行 child 配置的脚本。父仓库索引字节未变，但执行任意配置程序本身已违反只读/不运行辅助程序边界，脚本也可能做其他写入或联网。
- 与返工的关系：同一夹具、同一 Git 2.55.0，另用 `git show 66e7ca0:src/...` 得到的原始 Git/command 源码编译基线 harness，collect 后没有标记。没有修改任一提交的源码。这是本轮恢复 attributes 后可复现的回归，属于必须改 1 的剩余缺口，不是要求新增子模块详情功能或重新扩大上一轮建议范围。
- 修法方向：禁止摘要命令下钻子模块工作区，或确保实际进入的每个仓库也受到 filter 禁用保护。最小方向可考虑明确传 `--ignore-submodules=dirty`，保留 gitlink 提交变化统计；不能为了挡脚本直接忽略所有 gitlink 变化。仅在临时夹具对 Git CLI 验证了此选项：子模块仅脏文件时不执行脚本、numstat 为空；子模块 HEAD 前进且工作区仍脏时仍不执行脚本、返回 `1\t1\tchild`。未改实现。建议补一个直接 collect 父仓库的定向回归，检查子模块脚本标记不存在，并保留 gitlink 变化断言。

原先 info/global/worktree filter 漏洞的直接修复方向接受：命令行清空 clean/smudge/process 并设 required，使确实需要外部转换的增删行未知；已有相关测试本轮通过。racy stat 和含 `=` driver 的保守未知沿用主控裁决，不要求放宽统计含义。不过在上述子模块路径补齐前，第 1 项不能关闭，两份 README 的“所有 attributes 来源都不执行辅助程序”也尚不能成立。

### 必须改 2：已解决

`src/git.rs:177-189,222-234` 的统一调用入口对定位及后续查询都传 `--no-lazy-fetch`；不依赖可能被静默忽略的环境变量。假旧 Git 返回 129 时，locate 失败、整条结果为 None，与 `git unavailable` 一致；两份 README 和 DESIGN 已同步 Git 2.45 能力门槛。

本轮运行 `git_that_cannot_refuse_lazy_fetch_is_unavailable_and_missing_objects_stay_missing` 通过，覆盖旧 CLI 拒绝和本地部分克隆缺 blob 在采集后仍缺失。未实跑旧版二进制，也不把测试描述成跨所有历史版本验证。第 1 项中的外部脚本副作用是另一个待补边界，不否定此处 Git 自身禁止对象补取的修复。

### 必须改 3：已解决

`src/git.rs:185-189` 在 Git 专用路径收集并移除继承的 GIT_*；`src/command.rs:20-44` 的普通 run 传空移除列表，corral/drover 的原环境不受改变。隔离逻辑覆盖目录、索引、对象来源、配置注入和 Git trace，符合主控接受的方案。

本轮独立运行 tests/git_env.rs，通过同时污染 GIT_DIR、GIT_WORK_TREE、GIT_INDEX_FILE、GIT_TRACE 的检查：A/B 仍各自返回 branch-a/branch-b，B 的未跟踪计数为 1，trace 文件不存在。未发现本轮环境改造引入的目标归属回归。

### 必须改 4：已解决

空树 attr-source 已移除；内建 text/eol/binary 属性继续由 Git 处理，外部 filter 的未知值策略符合裁决。`tests/git.rs:231-247` 本轮通过：已提交的两行 CRLF 内容仅改 mtime，增删行仍为 0/0；带 binary 属性的修改文件独立计为 1。没有再以原始字节差异冒充未提交净增删。

恢复内建属性这一方向可以保留；它引出的子模块外部 filter 回归已集中列在第 1 项，不重复计为第 4 项未解决。

### 验证与变更边界

- 前台完成 `CARGO_TARGET_DIR=$HOME/Developer/personal_projs/saddle-worktrees/.target cargo test --test git --test git_env`：Git 6、git_env 1，共 **7 passed**。未重跑主控刚完成的 Agents/UI/corral/drover、全套或 clippy。
- `git diff --check 66e7ca0..bb38568` 通过。仅追加本节；不改代码或测试、不提交、不切分支，审查 worktree 保持 detached bb38568 且干净。
- 子模块验证仅用临时本地仓库和本地路径 clone，脚本只写临时标记；没有访问真实远端、操作真实 agent 或修改用户仓库配置。临时 harness/夹具于结束时清理，所有命令在前台结束。

## 主控裁决与第二轮返工

- 2026-09-26：认可第一轮返工交叉复核。必须改 2、3、4 已解决；必须改 1 剩余的子模块 filter 路径属于恢复 attributes 后引入的真实回归，有直接 collect 父仓库和脚本标记证据，仍阻挡合并。不重新打开已通过项目及此前建议。
- 交回 saddle/dev-t11-git-1，只修这一条。接受 `--ignore-submodules=dirty` 作为最小方向：父仓库摘要不进入子模块工作区，保留 gitlink 提交变化；子模块本身作为 agent 的 cwd 时仍按它自己的 worktree 正常统计。不要用忽略全部子模块变化掩盖 gitlink，也不要增加递归扫描或子模块详情功能。
- 先补父仓库直接 collect 的定向 RED→GREEN：子模块独有 filter 不执行；子模块只有未提交文件变化时不下钻，HEAD 前进时保留 gitlink 差异。保留原来有效断言，同步必要的 DESIGN/README 限制说明及原开发任务完成记录。
- 验证只跑相关 Git/git_env 检查、cargo clippy --all-targets -- -D warnings、git diff --check；使用临时本地仓库与脚本标记，不访问真实网络、用户仓库、配置或 agent，不重跑无关套件。
- 只在原 t11-agent-git 分支提交，不合并 main、不推送、不改本主仓库审查文件。回复修复、验证和取舍，命令全部前台结束，末行 DONE。主控收到后更新 detached 审查 worktree，再交原审查者进行第二轮返工复核；若第二轮复核仍不通过，按流程停止往返并向用户说明。

## 第二轮返工主控增量复核及复审请求

- 2026-09-26，主控读取开发者状态、回复，核对 `bb38568..2b60ab1` 全部增量；本轮仅剩的子模块 filter 缺口主控复核通过，合并仍等待原审查者第二轮增量复核。
- 实现只在 diff-index 增加 `--ignore-submodules=dirty`，另补说明和定向测试；接受父仓库不统计子模块未提交文件变化、仍保留 gitlink 提交变化的取舍。子模块作为独立 cwd 时仍按其自身配置阻止 filter；未扩大为递归扫描或子模块详情。DESIGN、两份 README 和开发完成记录一致。
- 主控前台运行 `cargo test --test git --test git_env`：8 passed，含父仓库不执行子模块 filter、gitlink 提交变化仍为 +1 -1、子模块自身查询返回未知且不执行 filter；`cargo clippy --all-targets -- -D warnings`、`git diff --check bb38568..HEAD` 通过。共用规定编译目录，没有重跑无关套件。
- 主控已将 detached 审查 worktree `../saddle-worktrees/review-t11-agent-git` 更新到 `2b60ab1d43f6b0f8f578b5032ee3c11d26cb231a`，状态干净。本轮源码和测试必须基于此提交。
- 交回原审查者 saddle/dev-t11-review-1（Codex gpt-6-astra / xhigh）：只复核 `bb38568..2b60ab1` 中剩余子模块 filter 缺口及本次改动引入的问题。必须改 2、3、4 已关闭，此前建议与取舍不重新打开。允许相关 Git/git_env 检查及必要的临时本地合成复现；不跑无关套件，不接触真实远端、用户仓库配置或 agent。
- 只在本主仓库文件末尾追加「## 第二轮返工交叉复核」，说明必须改 1 是否已完全解决，给出可以合并/改完再合并及条数。只读源码，不改代码/测试、不提交、不切分支；所有命令前台结束，回复末行 DONE。
- 若本次第二轮交叉复核仍未到「可以合并」，停止往返并向用户说明，不再自行派第三轮返工；通过后按用户授权合并、推送、构建更新 release、清理自有 worktree/分支/agent、补收尾记号与 HANDOFF，将 T11 标完成待放行。


## 第二轮返工交叉复核

可以合并。

2026-09-26，saddle/dev-t11-review-1：先核对 HEAD 为 `2b60ab1d43f6b0f8f578b5032ee3c11d26cb231a`、工作区干净；仅复核 `bb38568..2b60ab1` 的剩余子模块 filter 缺口及本次改动引入的问题。**剩余必须改 1 项已解决；本轮必须改 0 项，新增建议 0 项。** 原必须改 2–4 保持已关闭，旧建议和主控已批准的取舍不重开。

- **原必须改 1 可以关闭。** `src/git.rs:123-140` 在原有安全参数和 filter 覆盖之外增加 `--ignore-submodules=dirty`，阻止父仓库 diff-index 为子模块脏状态启动子模块内的 status/filter；未改成忽略全部子模块变化。此次实现改动只有该参数和解释注释，没有递归扫描、runner 或统计解析扩张。
- **定向回归覆盖了剩余缺口和保留行为。** 本轮实际前台运行 `CARGO_TARGET_DIR=$HOME/Developer/personal_projs/saddle-worktrees/.target cargo test --test git parent_summaries_never_enter_submodule_worktrees_but_keep_gitlink_changes -- --exact`：**1 passed，6 filtered out**。`tests/git.rs:347-405` 直接调用真实 collect，子模块独有 clean filter 的标记始终不存在：仅子模块文件未提交时父仓库为 `+0 -0`；子模块 HEAD 前进且仍有脏文件时父仓库为 `+1 -1`；子模块作为独立 cwd 时增删未知，filter 同样未执行。这与上一轮独立复现的触发条件和已验证的修法方向一致。
- **文档和范围一致。** DESIGN、两份 README 与第二轮完成记录均明确：父仓库不统计子模块内部未提交变化，保留 gitlink 提交变化；子模块作为独立 cwd 按自身配置统计。接受主控对这一最小方案的裁决。本次增量未发现新的阻塞问题。
- **验证边界。** `git diff --check bb38568..2b60ab1` 通过；主控已完成的 Git/git_env 8 项、clippy 及其他已关闭事项未重复验证。未接触真实远端、用户仓库配置或 agent，未改代码/测试、未提交、未切分支；只在本主仓库审查文件末尾追加本节，所有命令已在前台结束。

## 最终主控裁决

- 2026-09-26：核对原审查者 idle 状态、DONE 回复和第二轮复核，接受「可以合并」。必须改 1–4 全部关闭，主控与交叉审查均通过，准予合并 `2b60ab1`。
- 验证沿用初审标准检查、两轮相关增量回归与本轮子模块定向复核，不重复无关套件。此前慢目录、包装器后代、按路径统计重命名的取舍维持，不扩大本任务。
- 按用户授权合并推送、更新 release，清理本任务两个 worktree、开发分支与自有 agent；随后补收尾记号、HANDOFF，将 T11 标完成待放行，不自动开始 T4/T2。
