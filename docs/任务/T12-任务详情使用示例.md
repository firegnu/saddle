# T12：补充任务详情使用示例

2026-09-26，saddle/main 交给 saddle/dev-t12-doc（Codex gpt-5.6-luna / medium）。
路由：轻 / 交叉审查不要 / 影响面：看得见（路由三项均明确）。
你是被委派的 agent：照本文件做，不再派 agent。

## 用户原话与验收

> 用于体验刚完成的 Tasks 详情功能。新增一份简短中文文档 docs/任务详情使用示例.md，说明如何打开任务详情、查看状态与上次验收记录、观察约 5 秒刷新，以及按 Esc 返回列表。以现有功能为准，只写文档，不改代码。做完后按正常流程审查、合并和收尾。
> 做完：等我放行

## 先读

- AGENTS.md。
- README.zh-CN.md 的任务详情说明、docs/DESIGN.md 第 26 节；有不确定时只读 src/detail.rs 和相关队列接线核对。

## 工作位置和范围

- worktree：`/Users/firegnu/Developer/personal_projs/saddle-worktrees/t12-detail-example`，分支 `t12-detail-example`。
- 只新增 `docs/任务详情使用示例.md`，并在本任务文件追加完成记录。用简短中文操作步骤写现有功能，界面标签保持实际英文；不要把上次验收缓存说成本次执行，5 秒指查询返回后的刷新间隔。
- 不改功能代码、测试、其他文档或设计，不操作真实队列、agent、配置，不读 drover/corral 内部文件。不开始 T2。

## 验证与交付

- 只运行 `git diff --check`，并对照现有说明/界面标签检查一遍文档步骤；纯文档不跑 cargo 套件、不做录屏或新测试。
- 在本分支提交，不合并、不推送、不改主仓库。主控负责审查合并和收尾。
- 本文末尾追加「## 完成记录」：写了什么、核对了什么、取舍或未完成项。命令都在前台跑完，回复最后一行写 DONE。

## 完成记录

- 新增 `docs/任务详情使用示例.md`，用简短中文步骤说明打开详情、查看状态与 `Last check`、观察查询返回后约 5 秒刷新，以及按 `Esc` 或 `Back` 返回列表。
- 已对照 `README.zh-CN.md` 与 `docs/DESIGN.md` 第 26 节核对英文界面标签和详情刷新/返回语义；明确 `Last check` 是既有记录，不是本次执行的验收。
- 取舍：按任务要求只写文档，未改代码、测试或设计，也未操作真实队列和 agent；未完成项：无。
