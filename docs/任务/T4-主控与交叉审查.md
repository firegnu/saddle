# T4：主控与交叉审查

2026-09-26，saddle/main 交给 saddle/dev-t4-review（Codex gpt-6-astra / xhigh，只读交叉审查）。原路由：重 / 交叉审查要 / 影响面碰要害，后台生命周期、结果归属与输入焦点。
你是被委派审查者，不再派 agent。只在本文件追加审查意见，不写功能代码、不提交。

## 背景与主控初审

- 用户已确认并授权：点击任务后在原 Tasks 区域内切换列表/详情，Back/Esc 返回；右侧 agent 实时终端保持原样。详情打开时立即查，之后约每 5 秒后台刷新；返回列表停止详情查询。drover 公开 schema 1 已实施并发布，不审查或修改其仓库。
- 开发者 saddle/dev-t4-details-1（Claude opus[1m] / xhigh）的第一次因 API 断连未交付，恢复后完成：设计 `2edcf73`、实现 `643233e`。已核对 idle、末行 DONE、开发 worktree 干净和完成记录。当前尚未合并或发布 T4。
- 主控已阅读实现、相关差异与完成记录，范围符合任务；主控前台 `cargo test --all-targets`：**102 passed、2 ignored**，`cargo clippy --all-targets -- -D warnings`、`git diff --check main...HEAD` 通过。开发者调整旧弹层测试的翻页期望与新 Tasks 内页面尺寸一致，保留内容已滚出的断言，接受。
- 主控初审通过，最终合并等待本次交叉审查；不要把标准测试通过当成无需检查关键路径。

## 先读

- 你所在 worktree 的 AGENTS.md、docs/任务/T4-任务状态与过程详情.md 的「开发任务」及完成记录，docs/DESIGN.md 第 26 节。
- 最终公开契约只读：`/Users/firegnu/Developer/personal_projs/drover/docs/任务详情JSON接口.md`。schema 1 的 current/awaiting/history、当前重算/上次缓存/历史留存边界是已批准语义，不重新设计接口。
- src/detail.rs、src/drover.rs 新增 show/DetailWorker、src/queue.rs 详情目标与页面、src/app.rs 生命周期与输入接线、src/ui.rs，以及本轮相关测试。

## 固定审查范围

- 审查 worktree：`/Users/firegnu/Developer/personal_projs/saddle-worktrees/review-t4-task-details`，detached `643233e`，主控已创建；先核对 HEAD 和干净状态。
- 完整差异 `git diff 0057202..643233e`；只在自己的 detached worktree 读源码/运行必要检查，不与开发者共用 worktree，不切分支。
- 唯一可写文件是本主仓库绝对路径 `/Users/firegnu/Developer/personal_projs/saddle/docs/任务/T4-主控与交叉审查.md`。它不在审查 worktree 中；只追加，不覆盖主控内容。不改代码、测试、其他文档，不提交、不合并、不推送。

## 核心审查点

1. DetailWorker 在返回列表、换任务/项目、重开、退出时是否及时停止查询并取消，是否可能阻塞 UI 或让旧查询持续堆积；单次 60 秒预算与每次返回后约 5 秒刷新是否如实。区分普通 drover 直接子进程与派生/包装器边界，用实际调用路径判断影响，不自动要求 runner 重构。
2. 项目、任务 ID、页面序号以及结果通道的归属，列表重排、current→awaiting→history、pending 开始、读取失败与切项目时是否会把数据套给其他任务；按 ID 跟踪不应因位置变化失效。
3. schema 1 解析和展示：未知/错误/过期/快照警告不冒充成功或零值；awaiting 的 done 不被当前判据回退，历史不借用当前判据；last_check 只称上次/留存样本，路由来源和区间提交数不假归因。只读公开接口，无执行验收或队列写入。
4. Tasks 内展示、Back/Esc、滚动与返回位置、输入焦点；与现有 Viewer 接入/鼠标/弹层/忙碌操作的交互，不误送任务详情按键到 agent 或误触发队列动作。
5. 完成记录、README/DESIGN 和真实代码一致性，新增/修改的检查是否有效覆盖所称行为。与本次无关的旧问题只列建议，不展开修复。

## 验证预算与禁止事项

- 主控刚跑完标准套件和 clippy，本次不重复全套。按疑点运行直接相关 drover/queue/ui 目标或单个既有 workflow 正常路径即可；需要复现仅用临时合成 JSON、假的 drover/corral 和可自行清理的临时夹具。没有具体疑点不另做覆盖矩阵、缺陷注入、性能基准或录屏。
- cargo 使用 `CARGO_TARGET_DIR=$HOME/Developer/personal_projs/saddle-worktrees/.target`。不写审查 worktree 内的测试/源码，不为了证明而绕过公开 CLI 或读取真实 drover/corral 内部文件。
- 不操作真实队列、配置、登记、服务、用户 agent 或其他项目；不运行真实 agent 测试，不发送 corral 消息。不要按项目名/路径批量杀进程，只管理记录 PID 的自起临时进程。

## 主控对开发者取舍的初步意见

- 接受 60 秒总查询预算、返回后再等 5 秒、不堆叠；是否有真实取消阻塞问题留本次重点核查。沿用现有 runner，不凭罕见包装器后代组合强制扩大架构。
- 接受分区先显示状态/判据，沿用 Running/Awaiting/Done/Dropped 用词，显示本地时间且不加依赖；单击进入与按页滚动符合确认的界面。
- 接受在原操作执行中仍能 Enter 打开详情，已有操作完成后沿用反馈弹层/返回列表；需保持原目标和不误触发新操作。
- 用户未要求 PTY 录屏；现有一个合成正常路径足够，真实终端观感由用户在发布后查看，不以此阻挡本轮交付。

## 输出

在本文件末尾追加「## 交叉审查意见」：先一句「可以合并」或「改完再合并」。每项按必须改/建议改/可以不改说明位置、触发、影响和修法方向，并对上述取舍逐项表态。必须改仅限验收未达到或关键路径真实缺陷；建议不挡合并。

说明本次验证与边界。回复只写结论和条数；所有命令前台结束，最后一行 DONE。


## 交叉审查意见

可以合并。

2026-09-26，saddle/dev-t4-review。审查对象为 detached `643233eb1c4c86389642931536512e2a4f082045`，完整差异基线为 `0057202`；审查开始和追加前 worktree 均干净。结论：**必须改 0 条，建议改 3 条，可以不改 5 条**。下述建议不挡合并，没有发现符合已发布 schema 1 和实际接线条件的关键路径缺陷。

### 建议改（3 条，不挡合并）

1. **让归属测试的成功样本与实际打开的任务一致。** 位置：`tests/queue.rs:405–422`，关联 `src/queue.rs:220–235, 179–194`。触发：测试把 T4 从 current 移到 awaiting 后，`absorb` 已将列表选择跟到 T4，Esc/Enter 重开得到的第二个 key 仍是 T4（仅 seq 改变），但第 421 行注入 T5 的成功结果，随后只断言 `data.is_some()`。影响：旧 seq 和其他项目 key 的拒收断言有效，但最后一段不能证明新页面展示的仍是 T4，且夹具与公开 show 的按 ID 返回语义不一致。本次单独运行该测试通过，也说明现有断言没有发现这处夹具错配。修法方向：成功样本改回 T4/awaiting，并明确断言第二个 key 的 id、seq 变化和吸收后的 task.id；若想验证换任务，先显式选择 T5 再打开。实际 App 的独立 worker/通道按固定参数查询，没有找到会在正常 CLI 响应下产生这个错配的路径，因此不升级为功能阻挡项，也不要求额外建立覆盖矩阵。

2. **routing 为 null 时避免断言不存在路由文件。** 位置：`src/detail.rs:397–398`。触发：schema 1 在未配置目录、文件不存在、文件不可读或没有可解析路由时都可返回 null；当前一律写成 `none (no routed task file)`。影响：可能把“未取得路由”说成“没有路由文件”，但不影响任务状态、判据或队列操作。修法方向：改成中性的 `Unavailable / no routing information` 一类文案，不根据 null 推断具体原因；有值时的 `from the current task file` 来源标注可保留。不要求改变 drover 契约。

3. **收窄文档对必需字段校验的笼统承诺。** 位置：`docs/DESIGN.md` 第 26 节“加载与失败”、开发完成记录“字段完整”，关联 `src/drover.rs:292–398, 439–440`、`tests/drover.rs` 删除 completion 的负例。触发：这些结构是消费字段的投影，并非整个 schema 1 的完整校验器；例如 `Timing` 的全部字段都是 `Option`，Serde 会把缺键也解释为 None，`Completion.rows` 等亦然，若干未使用字段则没有被反序列化。影响：已验证的未知 schema、ok=false、非零退出、非 JSON、缺少整个 completion 对象确实会失败，但不能据此声称所有契约必有字段缺失都被拒绝。修法方向：把文档表述限定为实际校验的 schema/退出状态及消费结构所需字段；无需为本轮补造完整 schema 校验框架。合规响应下未知值仍显示 unknown/not recorded，不会因此伪造零或验收通过。

### 可以不改（5 条，含主控取舍逐项判断）

1. **接受 60 秒预算、返回后约 5 秒再查及沿用现有 runner。** 位置：`src/drover.rs:402–478`、`src/app.rs:330–352`、`src/command.rs:67–84`。打开详情启动一次 show，线程在 show 返回后才等待刷新间隔；没有并行堆叠查询。返回、换 key、重开时先 drop 原 worker，取消标记、停止通道和 join 一起结束该轮；直接子进程仍运行时，runner 每约 10ms 检查取消并 kill/wait，正常慢查询留在后台。既有定向测试用 `exec sleep 30` 覆盖了这种直接子进程挂起，Drop 在其 2 秒上限内返回。
   边界也需保留：runner 只杀直接子进程，不保证清掉派生进程；若自定义包装器先退出、后代仍持有输出管道，代码已进入 `recv_timeout` 时不再检查 cancel，UI 上的 drop/join 可能等到剩余 60 秒预算。该 runner 路径原已存在，本次实际接线是直接执行配置的 drover 命令，没有引入 shell 包装器；公开契约没有要求后台化或让后代持有其输出管道。未取得普通 drover 调用触发此特殊组合的证据，故不以此强迫 runner 重构。本次没有复现派生/包装器组合，也没有把直接子进程测试说成覆盖全部后代取消。

2. **接受项目/ID/页面序号与通道隔离方案。** 位置：`src/queue.rs:145–195`、`src/app.rs:319–352, 668–689`。列表重排及 current→awaiting→history 不改变页面保存的 ID；seq 区分重开，项目参与 key，App 更换 worker 后旧通道一并丢弃，吸收时再次核对 key。切项目重建 Panel，原页面及缓存不带过去。编号 pending 开始后，live 分组变化使它进入 show 路径；pending 和未编号任务不被误当成当前位置的另一个任务。目标从列表消失时，编号目标仍按原 ID 查询，查询失败保留旧数据并明确 stale，不回退到列表索引；未编号目标找不到时标注打开时副本。没有要求把消失目标改查成别的任务的理由，现有实现可保留。

3. **接受展示顺序、状态用词、本地时间和单击/按页交互。** 位置：`src/detail.rs:60–167, 178–286, 289–513, 670–695`、`src/queue.rs:855–907, 1445–1464`、`src/ui.rs:190–193`。状态来自 task 的位置/记录，awaiting 明确说明已记录 done 不因重算改变；history 按接口的 rows=null 展示快照未保存，Last check 和 retained_sample 与本次验收/完成快照分开。Git 区间计数、end HEAD、现在 main 顶端时间和有值路由来源均有边界说明；Hold 未知、关注推断、快照警告、加载/错误/旧数据均有对应文案。正文和 why 走统一普通文本净化/折行，不用来决定业务或执行指令。先状态和判据，再其余分区，沿用 Running/Awaiting/Done/Dropped、本地时间且不加依赖，符合主控取舍；不用重新设计接口或状态名称。Details 只占 Tasks 区域，正文刷新不重置 scroll，Back/Esc 保留列表状态；单击进入、PgUp/PgDn 按页处理可保留。

4. **接受忙碌操作期间可进入详情，以及原操作完成后的反馈行为。** 位置：`src/queue.rs:280–339, 376–386, 531–533, 692–693, 809–838`、`src/app.rs:411–446, 507–560`。Enter 打开详情不生成新队列写请求，detail 分支吞掉列表动作键，按钮也禁用；正在执行的操作仍使用原已捕获的 Operation，Go/Next 或失败以原反馈弹层替换页面，Move 成功回列表，下一 tick 取消详情 worker。详情键盘/粘贴在 Queue 焦点下不走 Viewer；滚轮命中 Tasks 后只改详情偏移，显式切 Viewer 后仍沿原规则输入。现有接入完成的焦点保护未被移除。这里无需另加锁或改变已批准操作目标语义。

5. **接受当前验证预算及不新增 PTY 录屏。** 位置：`tests/workflow.rs:1039–1093`、`tests/drover.rs:446–504` 及本轮 queue/ui 测试。正常集成用假的 corral/drover，覆盖单击进入、正确 show 参数、第二次刷新、已接入 Viewer 时详情键不透传、仅 list/show 调用，以及 Esc 后 6 秒没有新 show。worker 测试覆盖顺序调用和运行中直接子进程取消。queue/ui 检查对状态流转、旧 key 拒收、分区语义、错误/旧数据和滚动有有效断言；第 1 条指出的成功样本应在后续收紧。旧弹层测试改为 Tasks 页的翻页期望且保留原首行滚出的断言合理，不要求恢复旧页高。主控已有全套和 clippy 结果，本次不重跑；真实终端观感继续由用户发布后确认，不作为合并门槛。

### 本次验证与边界

- 已阅读仓库 AGENTS.md、开发任务及完成记录、DESIGN 第 26 节、drover 已发布的公开契约全文，并检查 `0057202..643233e` 的全部变更文件及涉及的现有 runner、焦点和反馈接线。README/DESIGN/完成记录总体与实现对应；第 3 条单列了校验承诺偏宽的问题。
- 前台运行并结束以下三个定向检查，均通过（合计 **3 passed**）：
  - `CARGO_TARGET_DIR=$HOME/Developer/personal_projs/saddle-worktrees/.target cargo test --test workflow clicking_a_task_opens_refreshing_details_in_the_tasks_area_until_back -- --exact`：1 passed，测试耗时 12.37s。
  - 同一 target 配置，`cargo test --test drover detail_worker_queries_one_at_a_time_and_stops_when_dropped -- --exact`：1 passed，测试耗时 1.12s。
  - 同一 target 配置，`cargo test --test queue details_follow_the_task_id_through_completion_and_ignore_older_targets -- --exact`：1 passed。
- `git diff --check 0057202..643233e` 通过。本次没有重跑全套/clippy，没有另建覆盖矩阵、缺陷注入、性能基准或录屏；60 秒上限、项目切换/退出和派生进程边界以代码审查为依据，未声称逐项完成动态复现。开发完成记录的 RED 历史没有重演。
- 未执行真实 drover/corral 调用，未读取其内部配置/队列/缓存/状态，未操作服务、用户 agent 或其他项目。只执行了现有测试的临时合成夹具，未修改代码、测试或审查 worktree 的任何文件；唯一文档写入为向本主仓库文件追加本节，无提交、分支切换、合并或推送。

## 主控最终裁决（2026-09-26）

**可以合并，无必须改项，不进入返工。** 已核对审查者 idle、DONE 和 detached `643233e`；主控在同一实现提交完整复跑 102 passed、2 ignored，clippy 与差异检查通过，交叉审查的三个相关检查通过。没有实现变更，不重复全套。

- 后台取消与归属：同意可以不改 1、2。直接 drover 调用的顺序刷新、取消及项目/ID/序号和通道隔离符合本轮要求；接受既有 runner 对特殊包装器后代持有输出管道的限制，不宣称所有派生进程都被清理。
- 状态/缓存语义：同意可以不改 3。当前重算、历史缺失、上次缓存和推断提示分开展示，awaiting 的已完成状态不会回退；界面顺序、用词和本地时间可接受。
- 输入焦点：同意可以不改 4、5。Tasks 内页面与返回位置、按键隔离、忙碌操作使用原目标及完成反馈均符合确认方案；既有集成与定向检查足够，真实字体观感由用户重启 release 后查看。
- 建议 1 保留为非阻挡建议：成功样本与目标不一致削弱该测试末段，但旧 key 拒收有效，实际 worker 固定按 ID 查询，不是正常路径归属缺陷。本轮不扩大为返工，后续若整理测试应按审查意见收紧。
- 建议 2 保留为非阻挡建议：routing=null 的原因文案过于具体，未影响状态、验收或动作。本轮不为文案延长开发往返，后续改为中性的不可用表述。
- 建议 3 采纳为收尾文档修正：将 DESIGN 和完成记录的校验承诺限定为实际消费结构，不把投影反序列化称为完整 schema 校验；不增加校验框架或改功能代码。

按用户授权合并、推送、构建更新 release，随后清理本任务两个 worktree、开发分支及两个自有 agent，补收尾记号与 HANDOFF；T4 标完成待放行，不推进 T2。
