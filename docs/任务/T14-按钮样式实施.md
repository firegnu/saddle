# T14：紧凑描边按钮与素雅 Tab

2026-09-26，saddle/main 交给 saddle/dev-t14-buttons（Claude Code，常规：opus[1m] / high）。
路由：常规 / 交叉审查不要 / 影响面：看得见（route.py：常规、不要、影响面拿不准；主控判断：纯样式及配套布局/命中几何，功能和会话状态机保持不变）。
你是被委派的 agent，不再开 agent。现在明确实施；旧队列正文的“先记录/只探索”已被用户最新指令覆盖。

## 用户原话 / 验收

> 弹出对话框内部的按钮改为清楚可辨的描边按钮：边框用白色稍偏灰，内部不填充背景色；尺寸紧凑，宽度贴合文字，不做大块按钮。
> 对话框底部的取消、返回等操作可以继续使用现有 ‹…› 紧凑样式。只调整外观，不改变按钮功能。

> 另外给你加一处，就是右侧agent区域的Tab那个我觉得也可以做成button的，但是这个要素雅，不能喧宾夺主。

主控盘点并展示 New/Split/Stop 及素雅 Tab 文字方案后，询问现在实施还是仅探索，用户回复：

> 不要待办，开干吧

## 先读与工作位置

- AGENTS.md、docs/DESIGN.md 第 30 节、docs/任务/T14-按钮样式盘点.md。
- src/buttons.rs、src/launch.rs、src/placement.rs、src/terminals.rs、src/ui.rs 和直接相关测试，按需只读 Queue 既有底栏。
- worktree：/Users/firegnu/Developer/personal_projs/saddle-worktrees/t14-outlined-buttons；分支 t14-outlined-buttons，由主控建立。
- 只改上述显示/布局所需代码、直接相关 tests、确需的 README、DESIGN 第 30 节实现取舍和本任务完成记录。不改 HANDOFF、旧任务审查记录或其他 worktree。

## 做什么

按 DESIGN 第 30 节落地已展示样式。New 内部操作和 Split 四方向换紧凑灰白描边，底栏保持单行；Stop 底部统一紧凑且保留危险色；右侧 Tab/+ 用素雅描边、当前项仅适度提亮和加粗、× 保持独立点击。透明指沿用面板背景、不新增按钮填充色。

复用现有控件与颜色，只调整必要布局和命中几何；同步处理 New 字段高度、Split 面板高度、tab 条三行与终端内容区域，不让按钮和实际点击位置脱节。现有选择、取消、表单编辑、会话移动与点击身份保护保持。不要因为所有位置都有 ‹…› 就全局替换。

## 验证预算

纯视觉无需伪造 RED。git diff --check 加直接体现新按钮/Tab 的合成渲染与命中几何检查；可调整并运行直接受尺寸/轮廓变化影响的现有断言，保留功能断言，不扩全套、矩阵、录屏或真实 agent 测试。检查方法在此预算内自行选择并记清实际观察。若 Cargo 命令，使用 `CARGO_TARGET_DIR=$HOME/Developer/personal_projs/saddle-worktrees/.target`。

## 不要做

不改创建、接入、取消、停止、移动、关闭语义或 PTY 生命周期，不加拖拽/命名/配置框架，不扩 Queue/Agents 主面板样式，不处理旧 Queue/Stop 偶发检查问题。若发现本轮引入真实行为错误应最小修正，不借机处理旧范围外问题。
不操作真实 agent、队列、用户配置或 release，不读 corral/drover 内部文件、不改其仓库。不按项目名批量杀进程；只停自己记录 PID 的测试进程。
不合并、不推送，只提交自己的分支。原 T2 已放行不重开，T14 由主控完成后按项目流程收结，不执行 go/next。

## 完成

本文件末尾追加完成记录：改了什么、直接渲染/命中检查结果、实现取舍、未做事项；提交并回复 SHA 与待裁决项。命令都在前台跑完，全部做完后，回复最后一行写 DONE。

## 完成记录（saddle/dev-t14-buttons，2026-09-26）

### 改了什么

- `src/buttons.rs`：新增 `draw_outlined_top`（顶部排布的三行圆角描边，无填充）；非紧凑普通按钮边框由 border 改为 muted（灰白），紧凑 ‹…› 不变。
- `src/launch.rs`：New 的 Project、Edit path、Codex/Claude、Advanced、Open in 及 Choose project 子页 Edit path 改用描边；段高与弹框高度同步（22→24，Advanced 38→40）；键盘焦点只着焦点色不填底。Create/Cancel/Back 底栏保持 ‹…›。
- `src/placement.rs`：Split 四方向描边、2×2，面板 6→9 行，Cancel 仍单行在底；新 tab 候选框起点随 tab 条下移。候选列表、Move here 未改。
- `src/ui.rs`：Stop 确认底部 Cancel/Stop 改为紧凑 ‹…›，Stop 保留危险色。
- `src/terminals.rs`：tab 条改三行（`STRIP`）：`+ Tab` 与各 `Tab N ×` 为细圆角描边无底色，非当前边框 border 灰、当前边框 muted 灰白且名称 bright 加粗；× 独立关闭命中；箭头保持文字；窗格区域与 PTY 尺寸从第 3 行以下计算。
- 测试：更新受尺寸/颜色直接影响的断言（`tests/buttons.rs` 描边静止色、`tests/terminals.rs` 窗格矩形 y 1→3 与 Split 文字、launch 弹框高度、`tests/workflow.rs` Stop 按钮文字 ` Stop y `→`‹Stop y›`）；新增 `tab_strip_and_split_sides_are_outlined_buttons_matching_their_targets` 与 `inner_controls_are_unfilled_outlines_matching_their_targets`。功能断言未删。

### 渲染 / 命中检查（实际观察）

- Tab 条 60×20 合成渲染：

  ```text
  ╭───────╮        ╭─────────╮ ╭─────────╮
  │ + Tab │  ‹  ›  │ Tab 1 × │ │ Tab 2 × │
  ╰───────╯        ╰─────────╯ ╰─────────╯
  ┏Viewer━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
  ```

  命中：`+ Tab` (0,0,9,3)；Tab 1 切换 (17,0,8,3)、关闭 (25,0,3,3)；Tab 2 切换 (29,0,8,3)、关闭 (37,0,3,3)；关闭区首列即 ×；前三行无背景色；当前 Tab 2 边框 Gray、名称 White 加粗，Tab 1 边框 DarkGray 不加粗。
- Split 面板：四个命中区高 3、两两同行，Cancel 高 1 紧贴其下；各框左上角为 ╭、颜色 muted。
- New（106×42，Advanced + Edit path + Codex 焦点）：六个内部命中区依次为 Ctrl-P、Ctrl-E、F2、F3、F4、F5，全部完整可见、互不重叠，四角 ╭/╯ 与命中矩形一致、框内无底色；Codex 焦点边框为焦点色；此外只有 Create/Cancel 两个单行命中。Choose project 子页 Edit path 为三行框，项目行从其下方开始。默认 New（100×32）渲染见 launch 测试输出，底栏 ‹Create agent Ctrl-S› ‹Cancel Esc› 仍在最底行。
- 运行：`cargo test --lib`（7 过）、`--test buttons`（4）、`--test terminals`（7）、`--test ui`（25）、`--test workflow` 中涉及 Stop/tab/Split/New 的 16 个（全过）；`cargo clippy --all-targets -- -D warnings` 通过；`git diff --check` 无输出。未跑全套 workflow 其余 22 个、未录屏、未用真实 agent。

### 实现取舍

见 DESIGN 第 30 节「实现取舍」条：焦点不填底、`+ Tab` 保留文字（设计线框只画了 +，保留文字与既有操作一致）、箭头不加框、放不下整框即不画不命中、悬停只提亮所在命中区（tab 主体与 × 各自提亮半个框）。

### 未做 / 待裁决

- 原 `buttons::draw`（底部三行变体）在 src 中已无调用，仅测试使用；按“不删既有公共函数”保留，是否删除请主控定。
- 悬停 tab 时只有被指区域（主体 8 列或 × 3 列）变亮，框会呈半亮；若希望整框一起亮需另加逻辑，本轮未做。
- `+ Tab` 与线框中的单个 `+` 不同，如要只显示 `+`，需同步改 workflow 测试的点击文字。

## 主控审查（2026-09-26）

- 结论：384339d 可以合并；无必须改项。范围限于样式、布局与命中几何，创建/取消/移动/关闭/点击身份及 PTY 生命周期逻辑未改；New/Split 内部描边、Stop 与其他底栏紧凑样式、素雅 Tab 符合任务。
- 验证：检查完整 diff 与开发完成记录，主控直接运行 New 内部描边和 Tab/Split 渲染命中两条检查，均通过；灰白圆角、无填充、底栏位置、独立 × 命中及互不重叠断言通过。静态核对 rects 同时供显示、PTY resize 和鼠标坐标使用。git diff --check 通过；开发报告的 lib/buttons/terminals/ui 与 16 条受影响 workflow、Clippy 均通过，主控按纯视觉预算未重跑套件，未使用真实 agent。
- 取舍 1 接受：New 键盘焦点仅提亮框和文字，避免三行底色块；取舍 2 接受：Tab 主体/× 分区悬停可显示当前点击目标，局部提亮不扩成整框。
- 取舍 3 接受：保留 + Tab，较裸 + 明确且沿用现有入口；取舍 4 接受：旧 buttons::draw 保留公共接口和既有检查，本轮不另做清理。接受三行 Tab 多占两行、空间不足不画完整按钮的既定布局取舍。
- 旧 Queue/Stop 偶发检查未定位、未修复，本轮没有扩展调查。原 T2 不重开，Agents diff 展示按用户最新决定保持现状；T14 收尾后待用户放行，不执行 go/next。
