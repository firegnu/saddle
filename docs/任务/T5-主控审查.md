# T5 主控审查

## 第一轮：需修正后再合并

2026-09-26，被审提交 b96235b。

- 已核对实现与文档范围；编辑/排序走公开 CLI，非 pending 不启用，公开快照预检查及其竞态限制已明确记录。继续接受任务书约定的预检查方案，不把其写成原子保护。
- 主控运行 `CARGO_TARGET_DIR=$HOME/Developer/personal_projs/saddle-worktrees/.target cargo test --all-targets`，新增 `pending_edit_and_move_buttons_preserve_draft_focus_and_selection` 失败，位置 tests/workflow.rs:481 的 `h.see("Second q")`。同目标其余 16 项通过、2 项忽略；前面的测试目标均通过。因为命令用 && 串联，Clippy 尚未执行。
- 失败输出的公开假 CLI 日志实际是 `["edit", "1", "Native queue task q", "detail line 0...Extra"]`，预期应编辑位置 2 的 `Second q`。失败界面仍在 Edit 表单，背景选中 T1，显示实际写入失败提示。这发生在正常选中/编辑验收路径，必须查清。
- 请原开发 agent 定位到底是产品选择保持还是 PTY 测试输入/屏幕同步导致，保留全部现有断言，不以重试或加固定 sleep 掩盖；修复后记录原因和对应证据。复核仅限这次改动与相关检查。
- 返工验证预算：先复现/解释上述失败，修好后运行相关检查；为排除已出现的不稳定，新增测试可有限重复 5 次，再执行一次标准 all-targets 和 Clippy。仍用临时配置/假 CLI，不动真实队列。主控不写功能代码。

## 第二轮：可以合并

2026-09-26，复核 2009da9 相对 b96235b 的增量。

- 根因和复现与第一轮失败相符：PTY 分块重绘过程中，旧表单的 Second 残留文字让测试误判队列已刷新；过早发送 j 后实际仍选中 T1，并非已选 T2 被产品重置。返工在 64 字节分块条件下复现同样失败，改为等待真实列表行及选中标记；原断言全部保留，无固定 sleep 或失败重试，产品代码未改。
- 开发者记录有限重复 5 次和完整标准测试 73 passed、2 ignored、Clippy 通过。主控只复跑受影响的 workflow：17 passed、2 ignored；补跑上轮尚未执行的 Clippy，通过；diff 检查通过。
- 同意仅 pending 显示编辑/调序入口、首末禁用、相邻移动、原表单复用、成功选择保持，以及公开快照预检查的实现。发现快照内容/顺序冲突时保留草稿并拒绝发送写命令。与任务书一致，跨进程原子保护的公开接口限制保留在文档；本任务不改 drover。
- 第一轮阻挡项已解决，可以合并。收尾时构建并更新 release，保留用户当前进程和配置，清理本任务 worktree/分支及所属 agent。
